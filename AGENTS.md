# AGENTS.md — FlashHook

> Auto-generated by the dev pipeline scanner. Do not edit manually.
> Last updated: 2026-07-12T13:55:22+09:00
> ⚠️ To update this file, architectural changes must be detected by the scanner and confirmed by a human.

## How to Read This File

Every agent in this pipeline reads this file before doing any work.
It defines the rules, patterns, and guardrails specific to this project.

## Stack Context

React 19 + Vite 8 + Zustand + CSS Modules + Spring Boot 4.0.7 (Java 21) + MongoDB + Redis

## Code Style Rules

- DO use TypeScript strict mode in the frontend.
- DO enforce code styles via ESLint, Husky, and lint-staged hooks.
- DO use Lombok annotations in the backend to minimize boilerplate.
- DON'T bypass existing validations (Zod in frontend, Spring Validation in backend).

## Architecture Guardrails

- This project separates frontend (SPA) and backend (REST API) completely.
- Frontend data fetching should strictly go through React Query and defined APIs in `FH_frontend/api`.
- Backend uses MVC Layers. Do not mix Service logic inside Controllers.
- All database interactions go through Spring Data MongoDB or Redis. Do not execute raw queries outside of repositories.

## Testing Requirements

- E2E tests are required for critical paths using Playwright.
- Backend architecture boundaries are enforced using ArchUnit. Do not violate layered architecture dependencies.

## Modularity Conventions

- Frontend API calls → `FH_frontend/api`
- Backend controllers → `Controller` layer
- Backend business logic → `Service` layer
- Backend data access → `Repository` layer

## Security Rules (All Agents)

- Never hardcode secrets, tokens, or credentials.
- Use environment variables for all sensitive config.
- Flag any auth-adjacent code changes immediately.

## Agent-Specific Instructions

### Orchestrator

- Does this change require coordination between both frontend and backend?
- If adding a new feature, verify that both the frontend data fetching layer and the backend endpoint align.

### Architect

- Be aware of performance implications when adding new data fetching. Use React Query's caching effectively.
- For backend, consider Redis caching for heavily accessed data.

### Developer

- Use React Query for all data fetching — no raw `fetch()` directly in components.
- Use Zustand for global UI state.
- In Spring Boot, utilize standard annotations (`@RestController`, `@Service`, `@Repository`).

### PR Reviewer

- Any change to `docker-compose.yml` or infrastructure files is automatically 🔴 Critical — requires human approval.
- Ensure ArchUnit tests pass for any backend refactoring.

### QA Agent

- Ensure all new features are covered by Playwright E2E tests.
- Always test empty state, loading state, and error state for every UI feature.
